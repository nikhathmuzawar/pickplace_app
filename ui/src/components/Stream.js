import React from 'react';
import WebRTCStream from './WebRTCStream';

const Stream = () => {
    return (
        <div>
            <h1>WebRTC with FastAPI and React</h1>
            < WebRTCStream/>
        </div>
    );
};

export default Stream;
